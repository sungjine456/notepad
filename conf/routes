POST        /user                   com.notepad.user.UserController.signUp
HEAD        /user/:id               com.notepad.user.UserController.exist(id: String)
GET         /users                  com.notepad.user.UserController.users

POST        /api/auth/signIn        com.notepad.security.SecurityController.signIn
GET         /api/auth/signOut       com.notepad.security.SecurityController.signOut

GET         /post/:idx              com.notepad.post.PostController.find(idx: Int)
POST        /post                   com.notepad.post.PostController.registered
POST        /post/:idx              com.notepad.post.PostController.updated(idx: Int)
GET         /posts                  com.notepad.post.PostController.posts
